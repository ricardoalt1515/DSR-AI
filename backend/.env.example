# ═══════════════════════════════════════════════════════════════════════════════
# DSR AI Platform - Backend Configuration
# ═══════════════════════════════════════════════════════════════════════════════
# Copy this file to .env and fill in your values
# NEVER commit .env to git - only commit .env.example
# ═══════════════════════════════════════════════════════════════════════════════


# ═══════════════════════════════════════════════════════════════════════════════
# 🔐 SECRETS (Required - Change these!)
# ═══════════════════════════════════════════════════════════════════════════════

# OpenAI API Key - Required for AI proposal and image analysis
# Get it from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# JWT Secret Key - Required for authentication
# Generate with: openssl rand -hex 32
# Must be at least 32 characters
SECRET_KEY=your-secret-key-min-32-chars-generate-with-openssl-rand-hex-32


# ═══════════════════════════════════════════════════════════════════════════════
# 🏠 APPLICATION
# ═══════════════════════════════════════════════════════════════════════════════

# Environment: development | staging | production
ENVIRONMENT=development

# Enable debug mode (detailed error messages)
DEBUG=true

# Application name (shown in API docs)
APP_NAME=DSR AI Platform

# Semantic version (update on each release)
APP_VERSION=1.0.0


# ═══════════════════════════════════════════════════════════════════════════════
# 🗃️ DATABASE - PostgreSQL
# ═══════════════════════════════════════════════════════════════════════════════

# Database credentials
POSTGRES_USER=h2o_user
POSTGRES_PASSWORD=h2o_dev_password          # Change in production!

# Database host (use 'postgres' for Docker, 'localhost' for local)
POSTGRES_SERVER=postgres
POSTGRES_PORT=5432
POSTGRES_DB=h2o_allegiant


# ═══════════════════════════════════════════════════════════════════════════════
# 📦 REDIS - Cache & Job Status
# ═══════════════════════════════════════════════════════════════════════════════

# Redis host (use 'redis' for Docker, 'localhost' for local)
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# Optional: Redis password (leave empty for local dev)
REDIS_PASSWORD=


# ═══════════════════════════════════════════════════════════════════════════════
# 🌐 API CONFIGURATION
# ═══════════════════════════════════════════════════════════════════════════════

# API version prefix
API_V1_PREFIX=/api/v1

# Backend URL (used for generating links in emails, presigned URLs)
BACKEND_URL=http://localhost:8000

# Frontend URL (used for password reset links, etc.)
FRONTEND_URL=http://localhost:3000

# CORS allowed origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001


# ═══════════════════════════════════════════════════════════════════════════════
# 💾 FILE STORAGE
# ═══════════════════════════════════════════════════════════════════════════════

# Use local storage (true for dev, false for production with S3)
USE_LOCAL_STORAGE=true

# Local storage path (relative to backend root)
LOCAL_STORAGE_PATH=./storage

# AWS S3 (only needed if USE_LOCAL_STORAGE=false)
# AWS_S3_BUCKET=your-bucket-name
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=             # Optional: uses IAM role if not set
# AWS_SECRET_ACCESS_KEY=         # Optional: uses IAM role if not set


# ═══════════════════════════════════════════════════════════════════════════════
# 🤖 OPENAI CONFIGURATION
# ═══════════════════════════════════════════════════════════════════════════════

# Model for AI proposals and analysis
# Options: gpt-4o, gpt-4o-mini, gpt-4-turbo
OPENAI_MODEL=gpt-4o-mini

# Temperature (0.0 = deterministic, 1.0 = creative)
OPENAI_TEMPERATURE=0.7

# Max tokens per response
OPENAI_MAX_TOKENS=4000


# ═══════════════════════════════════════════════════════════════════════════════
# 📎 FILE UPLOAD
# ═══════════════════════════════════════════════════════════════════════════════

# Max upload size in bytes (default: 10MB)
MAX_UPLOAD_SIZE=10485760

# Allowed file extensions (comma-separated)
ALLOWED_EXTENSIONS=.pdf,.docx,.xlsx,.xls,.jpg,.jpeg,.png,.txt


# ═══════════════════════════════════════════════════════════════════════════════
# 📝 LOGGING
# ═══════════════════════════════════════════════════════════════════════════════

# Log level: DEBUG | INFO | WARNING | ERROR | CRITICAL
LOG_LEVEL=INFO

# Log file path
LOG_FILE=logs/app.log


# ═══════════════════════════════════════════════════════════════════════════════
# 🔑 JWT AUTHENTICATION
# ═══════════════════════════════════════════════════════════════════════════════

# JWT algorithm (don't change unless you know what you're doing)
ALGORITHM=HS256

# Token expiration in minutes (default: 24 hours)
ACCESS_TOKEN_EXPIRE_MINUTES=1440


# ═══════════════════════════════════════════════════════════════════════════════
# 📧 EMAIL (Optional - for password reset, notifications)
# ═══════════════════════════════════════════════════════════════════════════════

# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=noreply@yourdomain.com


# ═══════════════════════════════════════════════════════════════════════════════
# 🔐 AUTH FLOW TOGGLES
# ═══════════════════════════════════════════════════════════════════════════════

# Enable public registration (false = invite-only)
AUTH_ENABLE_REGISTRATION=false

# Enable password reset via email
AUTH_ENABLE_PASSWORD_RESET=false

# Require email verification
AUTH_ENABLE_EMAIL_VERIFICATION=false


# ═══════════════════════════════════════════════════════════════════════════════
# 👤 INITIAL SUPERUSER (Optional - for first-time setup)
# ═══════════════════════════════════════════════════════════════════════════════

# Create admin user on first startup
# FIRST_SUPERUSER_EMAIL=admin@example.com
# FIRST_SUPERUSER_PASSWORD=your-secure-password


# ═══════════════════════════════════════════════════════════════════════════════
# 📊 MONITORING (Optional)
# ═══════════════════════════════════════════════════════════════════════════════

# Sentry DSN for error tracking
# SENTRY_DSN=https://xxx@sentry.io/xxx


# ═══════════════════════════════════════════════════════════════════════════════
# 🚀 PRODUCTION NOTES
# ═══════════════════════════════════════════════════════════════════════════════
#
# For AWS deployment, set these via AWS Console/Secrets Manager:
#
# ENVIRONMENT=production
# DEBUG=false
# POSTGRES_SERVER=your-rds-endpoint.us-east-1.rds.amazonaws.com
# REDIS_HOST=your-elasticache-endpoint.cache.amazonaws.com
# USE_LOCAL_STORAGE=false
# AWS_S3_BUCKET=your-bucket-name
# AWS_REGION=us-east-1
# CORS_ORIGINS=https://your-frontend.amplifyapp.com
# BACKEND_URL=https://your-api.awsapprunner.com
#
# Secrets (via AWS Secrets Manager):
# - OPENAI_API_KEY
# - SECRET_KEY
# - POSTGRES_PASSWORD
#
# ⚠️ After deploy, run migrations:
#   alembic upgrade head
#
# ═══════════════════════════════════════════════════════════════════════════════
